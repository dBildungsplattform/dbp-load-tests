apiVersion: v1
kind: ConfigMap
metadata:
  name: configmap-moodle-options
  namespace: loadtest
data:
  300s100u.json: |-
{{ .Files.Get "config/options/300s100u.json" | indent 4}}
  300s250u.json: |-
{{ .Files.Get "config/options/300s250u.json" | indent 4}}
